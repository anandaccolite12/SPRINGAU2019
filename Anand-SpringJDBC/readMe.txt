Using Spring JDBC
	* User can store student detail in database.
	* User can view particular student record by Student-ID (Using RowMapper)
	* User can view all students in the database ( Using resultSet extractor)
	
To Run application : run MainClass.java